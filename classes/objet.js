class objet{

   constructor(nom) {
       this.nom = nom;
       this.effet = effet;
       this.quantite = quantite;

   }

   get nom(nom){
       return this.effet;
   }

    get effet(effet){
        return this.effet;
    }
    get quantite(quantite){
        return this.effet;
    }

    set nom(nom){
        return this.effet;
    }
    set effet(effet){
        return this.effet;
    }
    set quantite(quantite){
        return this.effet;
    }
}
